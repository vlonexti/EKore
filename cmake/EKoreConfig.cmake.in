@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/EKoreTargets.cmake")

if(TARGET EKore::EKore AND NOT TARGET EKore::Library)
    add_library(EKore::Library ALIAS EKore::EKore)
endif()

check_required_components(EKore)

